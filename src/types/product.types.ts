export interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  currency: string;
  cost: number;
  costCurrency: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}
