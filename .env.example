# Database
DATABASE_URL="file:./dev.db"

# Authentication
AUTH_SECRET="your-secret-key-here"

# Currency API Integration
# Get your free API key from: https://api-ninjas.com/ (for crypto prices)
API_NINJAS_KEY="your-api-ninjas-key-here"
# Get your free API key from: https://freecurrencyapi.com/ (for FIAT exchange rates)
FREE_CURRENCY_API_KEY="your-free-currency-api-key-here"

# Blockchain API Integration
# Get your API key from: https://www.alchemy.com/ (supports Ethereum, Solana, Bitcoin, BNB Smart Chain)
ALCHEMY_API_KEY="your-alchemy-api-key-here"
# Get your API key from: https://cryptoapis.io/ (fallback for other blockchains)
CRYPTO_APIS_KEY="your-crypto-apis-key-here"

# Etherscan API Integration (for ETH and BNB Smart Chain transactions)
# Get your free API key from: https://etherscan.io/apis (works for both Etherscan and BSCScan)
ETHERSCAN_API_KEY="your-etherscan-api-key-here"
# Optional: Separate BSC API key from: https://bscscan.com/apis (falls back to ETHERSCAN_API_KEY)
BSCSCAN_API_KEY="your-bscscan-api-key-here"

# TronGrid API Integration (for Tron TRX and TRC-20 transactions and balances)
# Get your free API key from: https://www.trongrid.io/ (100K requests/day, 15 QPS limit)
# Without API key, requests are severely rate-limited and may fail with 403 errors
TRONGRID_API_KEY="your-trongrid-api-key-here"

# Redis Cache (optional - leave empty to disable and use in-memory cache)
# Set to empty string or "false" to disable Redis and avoid connection warnings
REDIS_URL=""
# Or configure Redis if available:
# REDIS_URL="redis://localhost:6379"

# Other environment variables...